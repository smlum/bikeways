# Copy to .env (gitignored) and fill in real values.
# Not required unless lode.config.yaml's tools.classification.street_view.mock
# is set to false — see the classification tool's Street View section.
GOOGLE_MAPS_API_KEY=
